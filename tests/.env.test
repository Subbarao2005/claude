# TEST ENVIRONMENT VARIABLES
# Replace placeholder values with your actual production credentials before running tests.

# Frontend & Backend URLs (update after deploying)
E2E_BASE_URL=https://melcho.vercel.app
API_BASE_URL=https://melcho-api.onrender.com

# Admin credentials (MUST match the seeded admin in your database)
ADMIN_EMAIL=admin@melcho.com
ADMIN_PASSWORD=ReplaceWithRealAdminPassword

# Test user credentials (used for E2E global setup)
TEST_USER_EMAIL=e2etest@melcho.com
TEST_USER_PASSWORD=TestPass123!
